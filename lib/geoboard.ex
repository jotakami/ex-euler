# defmodule Geoboard do

  # defmodule Corner do
    # defstruct [:pins, :blanks, :area]
    # @type t :: %Corner{}
  # end
  
  # @spec corners(pos_integer, pos_integer) :: [Corner.t, ...]
  # def corners(x, y), do: corners(x, y, 0, 1, x, 0)
  # def corners(x, y, slope, pins, blanks, area) do
    # # do stuff
  # end
  
  # def interior_pins(x, y) when y == 1, do: 0
  # def interior_pins(x, y) when y > x, do: interior_pins(y, x)
  # def interior_pins(x, y) do
    # IO.puts "blarg"
  # end
# end